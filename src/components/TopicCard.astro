---
import type { Topic } from "../data/topics.ts";

type Props = Topic;

const { title, techs } = Astro.props;

const formatTechs = (values: string[]) =>
  values.toString().replaceAll(",", " • ");
---

<>
  {(
    <div
      class="flex flex-col gap-1 p-1 "
    >
      <div>
        <p class="text-lg text-neutral-950">{title}</p>
      </div>

      {/* Description and locatime below */}
      <div class="flex flex-col gap-1">
        <div class="flex flex-col gap-4 md:flex-row md:items-center">
          <p>{formatTechs(techs)}</p>
        </div>
      </div>
    </div>
  )}
</>


