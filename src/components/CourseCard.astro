---
import type { Experience } from "../data/experience.ts";

type Props = Experience;

const { title, techs, link, institution, description, locatime, category } = Astro.props;

const formatTechs = (values: string[]) =>
  values.toString().replaceAll(",", " • ");
---

<>
  {(
    <a
      class="group cursor-pointer flex flex-col gap-1 rounded-md p-4 transition-all duration-300 hover:-translate-y-1 hover:border-neutral-950"
      href={link}
      target="_blank"
      rel="noreferrer"
    >
      <div>
        <p class="text-lg text-neutral-950">{title}</p>
        <p class="text-neutral-750">{institution}</p>
      </div>

      {/* Description and locatime below */}
      <div class="flex flex-col gap-1">
        <p class="text-sm text-neutral-750">{description}</p>
        <p class="text-sm text-neutral-750">{locatime}</p>
      </div>
    </a>
  )}
</>


